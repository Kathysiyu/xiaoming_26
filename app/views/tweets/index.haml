%i.fa.fa-cloud{:style => "font-size:60px;color:lightblue;"}
.center
  = link_to '投稿', new_tweet_path, id: 'create-tweet-btn'

.center
  - @tweets.each do |t|
    = render 'layouts/tweets', nickname: t.user.nickname, t: t
  = paginate @tweets
